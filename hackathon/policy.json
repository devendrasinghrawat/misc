{
        "services":[
      {
           "name": "dns",
           "status": "active",
           "image_src": "XXXXXXXXXXXXXXXXX",
           "docker-compose_config": "json format file, convert to yaml at runtime",
           "docker-compose_start_params": "optional"
      },
      {
           "name": "security",
           "status": "inactive",
           "image_src": "YYYYYYYYYYYYYYYYYYYYYYYYY",
           "docker-compose_config": "json format file, convert to yaml at runtime",
           "docker-compose_start_params": "optional"
      }]
}
